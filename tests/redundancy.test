{
    int x = 1;

    while (x < 10)
    {
        int y = x;
        //int a = x;
        print(y);
        //a = x + 1;
        // Oops! Stupid programming error, calculating duplicate values!
        y = x + 1;
        x = y;
        // And another! Very silly of me
        //x = x;
    }
}

{
    int x = 1;

    while (x < 10)
    {
        int y = x;
        print(y);
        x = x + 1;
        // Oops! Stupid programming error 
        x = x;
        // And another! We don't even care!
        x = y + 2;
    }
}
